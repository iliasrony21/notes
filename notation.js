const person ={
    name:'rony',
    'name-one':'rabbi',
    phone:'samsung',
}

const dot = person.name;
const bracket = person['name'];
// const dot3 = person.name-one; //not use dot notation
const bracket3 = person["name-one"]; // use only bracket notation
console.log(bracket3);